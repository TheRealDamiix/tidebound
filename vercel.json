{
  "buildCommand": "pnpm --filter @tidebound/shared build && pnpm --filter @tidebound/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
